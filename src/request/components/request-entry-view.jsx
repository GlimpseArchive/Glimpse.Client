var React = require('react');

module.exports = React.createClass({
    render: function() {
        return (
            <div className="request-session-holder">
                <h2>Request</h2>
                <em>No found requests.</em>
            </div>
        );
    }
});
